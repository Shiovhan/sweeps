<%= form_for(@sweep) do |f| %>
  <% if @sweep.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sweep.errors.count, "error") %> prohibited this sweep from being saved:</h2>

      <ul>
      <% @sweep.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_area :title %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.text_field :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
